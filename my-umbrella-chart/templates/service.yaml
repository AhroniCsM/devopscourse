apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-umbrella-chart.fullname" . }}
  labels:
    {{- include "my-umbrella-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-umbrella-chart.selectorLabels" . | nindent 4 }}
